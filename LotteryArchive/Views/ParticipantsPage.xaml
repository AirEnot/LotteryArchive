<Page x:Class="LotteryArchive.Views.ParticipantsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="ParticipantsPage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Style="{StaticResource PageTitleStyle}"
                   Text="Участники"
                   Margin="0,0,0,12" />

        <Grid Grid.Row="1">
            <TextBlock x:Name="EmptyParticipantsText"
                       Visibility="Collapsed"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="18"
                       Foreground="Gray"
                       Text="Участники пока не созданы" />

            <ListBox x:Name="ParticipantsList"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     HorizontalContentAlignment="Stretch"
                     SelectionChanged="ParticipantsList_OnSelectionChanged">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,8" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding FullName}"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1"
                                           VerticalAlignment="Center"
                                           Text="{Binding Balance, StringFormat=Баланс: {0}}" />
                                <TextBlock Grid.Column="2"
                                           VerticalAlignment="Center"
                                           Text="{Binding Greed, StringFormat=Жадность: {0}}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Border x:Name="EditPanel"
                Grid.Row="2"
                Style="{StaticResource CardBorderStyle}"
                Margin="0,0,0,12"
                Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="SelectedParticipantText"
                           Grid.Row="0"
                           FontWeight="SemiBold"
                           FontSize="16"
                           Margin="0,0,0,12"
                           Text="Редактирование участника" />

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Баланс" FontWeight="SemiBold" />
                        <TextBox x:Name="BalanceEditBox"
                                 Margin="0,6,0,0"
                                 PreviewTextInput="NumericBox_OnPreviewTextInput" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Grid>
                            <TextBlock Text="Жадность" FontWeight="SemiBold" />
                            <TextBlock x:Name="GreedValueText"
                                       HorizontalAlignment="Right"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBrush}" />
                        </Grid>
                        <Slider x:Name="GreedSlider"
                                Margin="0,8,0,0"
                                Minimum="0"
                                Maximum="100"
                                TickFrequency="1"
                                IsSnapToTickEnabled="True"
                                ValueChanged="GreedSlider_OnValueChanged" />
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0"
                            MinWidth="160"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Content="Удалить участника"
                            Click="DeleteParticipantButton_OnClick" />
                    <Button Grid.Column="2"
                            MinWidth="180"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Content="Сохранить изменения"
                            Click="SaveChangesButton_OnClick" />
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource BottomBarStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Content="Назад"
                        Click="BackButton_OnClick" />
                <Button Grid.Column="2"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Content="Добавить участника"
                        Click="AddParticipantButton_OnClick" />
            </Grid>
        </Border>
    </Grid>
</Page>
